# 다음 펠로우십 이어가기 가이드 (개발 재개 지침서)

안녕하세요! 진행하시던 **'다음 펠로우십 주인공(주제) 투표 및 제안 기능'** 화면 UI 작업 내용을 안전하게 깃(Git)에 커밋해 두었습니다. 언제든지 아래 순서대로 실행하시면 바로 이어서 개발을 완료하실 수 있습니다.

## 1. 현재 완료된 상태
- **투표 및 제안 UI 구현 완료:** `Episode.tsx`에 투표율(%) 게이지 바, 주제 투표 모달, 새 주제 제안 모달 기능 등의 UI 로직 작업이 구조화되어 있습니다.
- 관련 파일 변경사항은 `feat(ui): implement topic voting and proposal UI in Episode.tsx` 라는 이름으로 안전하게 Commit 되었습니다.

## 2. 다음에 이어서 하실 작업 (백엔드/DB 연동)
현재 투표 데이터가 임시 메모리(`localStorage`)에 저장되고 있습니다. 이를 **Turso DB(실제 데이터베이스)**에 연동하고 **Vercel API**를 통해 소통하도록 변경하는 작업이 남았습니다. 

## 3. 재개 시 실행할 명령어

터미널(Terminal)을 열고, 프로젝트 폴더(`the Next Fellowship`)에서 다음 명령어들을 순서대로 복사해서 실행해 주세요!

### 단계 1: Turso DB 및 Vercel 로그인
제가 대신 DB를 생성하고 코드를 연동해드리기 위해서는 CLI 인증 권한이 필요합니다.
```bash
# 1. Turso 데이터베이스 CLI 로그인 (브라우저가 열리면 인증 완료해주세요)
turso auth login

# 2. Vercel 서버리스 배포 CLI 설치 및 로그인 (브라우저가 열리면 인증 완료해주세요)
npm install -g vercel
vercel login
```

### 단계 2: 개발 서버 켜기 및 AI 에이전트 호출
```bash
# 로컬 개발 서버를 다시 켭니다. 
npm run dev
```

이후 채팅창에 **"로그인 다 했어! 백엔드 연동이랑 배포 이어서 진행해줘"** 라고 말씀해주시면, 제가 기억하고 있던 계획(`implementation_plan.md`)에 따라 즉시 다음 작업을 수행하겠습니다.

*준비가 되시면 언제든지 다시 불러주세요! 🚀*
